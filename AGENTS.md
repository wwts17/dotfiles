# 项目规范

## 配置风格

- 沿用 Homebrew + Stow，直接维护应用原生配置；按工具组织文件。
- 配置以可读性为先。优先使用语言内置能力，仅在确实简化调用时提取辅助函数。
- 注释简短说明原因、顺序约束或兼容性问题。删除复述代码的说明、装饰性标题和注释掉的示例。
- 用简短分组和空行组织配置；详细使用说明放文档，README 只保留用途、入口和必要链接。
- 整理与注释清理保持配置值及执行行为不变；行为优化单独说明影响并验证。

## Zsh

- `.zprofile` 负责登录环境和 Homebrew 初始化；`.zshrc` 负责交互功能、插件和工具初始化。
- 用 `path` / `fpath` 数组管理路径，用 `typeset -U` 对相应标量与数组去重；明确路径优先级。
- 用 `(( $+commands[name] ))` 检测外部程序；需要识别函数或别名时使用 `command -v`。
- 用 `[[ ... ]]` 做条件判断，路径引用加引号，以 `$HOME` 代替写死的用户目录。
- 调整补全时，同时检查 `fpath` 注册、`compinit` 和插件调用 `compdef` 的顺序。
- 本机覆盖文件最后加载；工具缺失时保持 shell 可启动。

## 验证

- 修改后运行 `git diff --check`，并对涉及的文件执行对应语言的语法检查。
- 修改 Bash 脚本时运行 ShellCheck；修改 Zsh 初始化行为时，在临时 HOME 中验证路径顺序、补全和重复加载。
- 验证优先使用语法检查、隔离环境和 dry-run；安装脚本用于实际部署，不作为检查命令。
